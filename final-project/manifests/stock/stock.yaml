apiVersion: v1
kind: ConfigMap
metadata:
  name: stock-cm
  
data:
  username: 'otus-project'
  url: 'jdbc:postgresql://stock-postgresql.default.svc.cluster.local:5432/postgres?prepareThreshold=0'
  applicationName: 'stock'
---
apiVersion: v1
kind: Service
metadata:
  name: stock-service
  
spec:
  selector:
    app: stock
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-deployment
  
  labels:
    app: stock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock
  template:
    metadata:
      labels:
        app: stock
    spec:
      containers:
        - name: stock
          image: olegmeditskiy96/otus-final-stock:1.6
          ports:
            - containerPort: 8000
          env:
            - name: MANAGEMENT_SERVER_PORT
              value: '8000'
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: stock-cm
                  key: username
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: stock-cm
                  key: url
            - name: SPRING_APPLICATION_NAME
              valueFrom:
                configMapKeyRef:
                  name: stock-cm
                  key: applicationName
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: otus-homework-secret
                  key: password
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: kafka-cm
                  key: kafka-host
          volumeMounts:
            - name: config
              mountPath: "/config"
              readOnly: true
      volumes:
        - name: config