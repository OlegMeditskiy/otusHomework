global:
  postgresql:
    auth:
      username: "otus-hw-8"
      existingSecret: "otus-homework-secret"
      secretKeys:
        userPasswordKey: "password"
auth:
  enablePostgresUser: false
persistence:
  enabled: false
